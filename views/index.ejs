<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="styles/main.css">
</head>
<body>
<header class="p-3 mb-3 border-bottom">
    <div class="container">
        <div class="d-flex flex-wrap align-items-center justify-content-center">
            <h1> Weather Forecast </h1>
            <form action="/submit" method="POST" class="d-flex flex-wrap align-items-center justify-content-center" role="search">
                <input type="text" value="" name="input" class="form-control" placeholder="Enter Zipcode" aria-label="Search">
                <input type="submit" class="btn btn-primary btn-lg" value="Search">
            </form>

        </div>
    </div>
</header>

<div class="container px-4 py-5">
    <%if(locals.location) { %>
    <h2 class="pb-2 border-bottom"><%= location.name%></h2>
    <% } %>
    <div class="row row-cols-1 row-cols-md-2 align-items-md-center g-5 py-5">
        <div class="col d-flex flex-column align-items-start gap-2">
            <h2 class="fw-bold text-body-emphasis">Current Temperature</h2>
            <% if (locals.result) { %>
            <p class="text-body-secondary"><%= result.current.temp%></p>

        </div>

        <div class="col">
            <div class="row row-cols-1 row-cols-sm-2 g-4">
                <div class="col d-flex flex-column gap-2">
                    <h4 class="fw-semibold mb-0 text-body-emphasis">Daily  High</h4>
                    <p class="text-body-secondary"><%= result.daily[0].temp.max%></p>
                </div>

                <div class="col d-flex flex-column gap-2">
                    <h4 class="fw-semibold mb-0 text-body-emphasis">Daily Low</h4>
                    <p class="text-body-secondary"><%= result.daily[0].temp.min%></p>
                </div>

                <div class="col d-flex flex-column gap-2">
                    <h4 class="fw-semibold mb-0 text-body-emphasis">Humidity</h4>
                    <p class="text-body-secondary"><%= result.current.humidity%></p>
                </div>

                <div class="col d-flex flex-column gap-2">
                    <h4 class="fw-semibold mb-0 text-body-emphasis">Dew Point</h4>
                    <p class="text-body-secondary"><%= result.current.dew_point%></p>
                </div>
                <% }%>
            </div>
        </div>
    </div>
</div>

</body>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</html>